<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <RootNamespace>WinFormsX.Samples</RootNamespace>
    <IsPackable>false</IsPackable>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <CheckForOverflowUnderflow>true</CheckForOverflowUnderflow>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <RunAnalyzers>false</RunAnalyzers>
    <EnableNETAnalyzers>false</EnableNETAnalyzers>
    <EnforceCodeStyleInBuild>false</EnforceCodeStyleInBuild>
  </PropertyGroup>

  <!-- WASM-specific settings -->
  <PropertyGroup Condition="'$(UseWasm)'=='true'">
    <RuntimeIdentifier>browser-wasm</RuntimeIdentifier>
    <OutputType>Exe</OutputType>
    <WasmMainJSPath>wwwroot\main.js</WasmMainJSPath>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\System.Windows.Forms\src\System.Windows.Forms.csproj" />
  </ItemGroup>

  <ItemGroup Condition="'$(UseWasm)'!='true'">
    <WinFormsXImpellerNative Include="..\..\artifacts\impeller\windows-x64\*\lib\impeller.dll" />
    <Content Include="@(WinFormsXImpellerNative)"
             Link="impeller.dll"
             CopyToOutputDirectory="PreserveNewest"
             CopyToPublishDirectory="PreserveNewest" />
    <Content Include="@(WinFormsXImpellerNative)"
             Link="runtimes\win-x64\native\impeller.dll"
             CopyToOutputDirectory="PreserveNewest"
             CopyToPublishDirectory="PreserveNewest" />
    <Content Include="..\System.Drawing.Common\src\System\Drawing\Fonts\**\*.*"
             Link="System\Drawing\Fonts\%(RecursiveDir)%(Filename)%(Extension)"
             CopyToOutputDirectory="PreserveNewest"
             CopyToPublishDirectory="PreserveNewest" />
  </ItemGroup>

  <Target Name="ValidateWinFormsXNativeDependencies"
          BeforeTargets="ResolveReferences"
          Condition="'$(UseWasm)'!='true' and '@(WinFormsXImpellerNative)'==''">
    <Error Text="WinFormsX.Samples requires a built Impeller native binary. Expected artifacts\impeller\windows-x64\*\lib\impeller.dll under the winforms repository root." />
  </Target>

  <!-- Copy wwwroot content to WASM AppBundle -->
  <ItemGroup Condition="'$(UseWasm)'=='true'">
    <WasmExtraFilesToDeploy Include="wwwroot\index.html" />
    <WasmExtraFilesToDeploy Include="wwwroot\main.js" />
  </ItemGroup>

</Project>
